package april.day18.ex;

public class CounterApp { // 카운터 증가, 감소 애플리케이션
    public static void main(String[] args) {
        // Thread incrementThread = new Thread(new IncrementCounter)); // 숫자를 1에서 100까지 증가시키는 스레드 객체 생성
        // Thread decrementThread = new Thread(new DecrementCounter)); // 숫자를 100에서 1까지 감소시키는 스레드 객체 생성

        // incrementThread.start();
        // decrementThread.start();
    }
}