package april.day17.ex;

/*
public class Application {
    public static void main(String[] args) {
        final String FILE_PATH = "texts.txt";
        TextFileManager fileManager = new TextFileManager(FILE_PATH);
        UserInputHandler inputHandler = new UserInputHandler();

        try {
            List<String> userInput = inputHandler.getUserInput();
            fileManager.writeToFile(userInput);

            List<String> fileContent = fileManager.readFromFile();
            System.out.println("Content of the file:");
            fileContent.forEach(System.out::println);
        } catch (IOException e) {
            System.err.println("An error occurred: " + e.getMessage());
        }

    }
}
*/